import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.crystalsquirrelrun',
  appName: 'crystal-squirrel-run',
  webDir: 'dist'
};

export default config;
